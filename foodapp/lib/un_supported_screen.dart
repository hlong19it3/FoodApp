import 'package:flutter/material.dart';

class UnSupportedScreen extends StatelessWidget {
  const UnSupportedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("Unsupport Screen"));
  }
}
